You are an autonomous Senior Software Engineer evaluating an on-demand code refactor request.
Target Issue: #384 - [OpenCode Technical Debt] Magic number colors: hardcoded hex/RGBA badge values bypass design token system

### REFACTOR BLUEPRINT REPORTED:

### Source
This issue was auto-detected from merged Pull Request #382.

### Location
Found in `style.css`, lines 98–100 and 103–105 (and duplicated at lines 138–140, 143–145).

### Description
PR #382 introduces six hardcoded color values that bypass the project's CSS custom property design token system:

- `rgba(168, 85, 247, 0.12)` — purple background
- `#7c3aed` — purple text
- `rgba(168, 85, 247, 0.3)` — purple border
- `rgba(16, 185, 129, 0.12)` — emerald background
- `#059669` — emerald text
- `rgba(16, 185, 129, 0.3)` — emerald border

The rest of the stylesheet follows a pattern of defining colors as CSS custom properties in `:root` and referencing them via `var()` (e.g., `var(--purple-400, #c084fc)` at line 415, `var(--color-brand)` at line 189). These new values are raw literals with no central definition, making them inconsistent with the established design system.

If the brand or badge colors need to change, these values are scattered across multiple locations (including the duplicated blocks) and will be easy to miss.

### Suggested Refactor
Define new CSS custom properties in the `:root` block for badge colors:

```css
:root {
  /* Badge tokens */
  --badge-purple-bg: rgba(168, 85, 247, 0.12);
  --badge-purple-text: #7c3aed;
  --badge-purple-border: rgba(168, 85, 247, 0.3);
  --badge-emerald-bg: rgba(16, 185, 129, 0.12);
  --badge-emerald-text: #059669;
  --badge-emerald-border: rgba(16, 185, 129, 0.3);
}
```

Then reference `var(--badge-purple-bg)` etc. in the light-mode overrides. This centralizes the values and aligns with the existing design token architecture.

### YOUR MISSION:
Analyze the corresponding source files in the current repository. Determine if this reported code smell is a genuine defect/anti-pattern that needs correcting, OR if this structure is intentional (e.g., required for performance, compliance with third-party APIs, or an explicitly guarded architectural design).

### EXECUTION DIRECTIONS (CHOOSE EXACTLY ONE PATH):

PATH A: [VALID CODE SMELL - FIX IT]
If the issue is correct and should be refactored:
1. Checkout a new branch named 'opencode/fix-issue-384'.
2. Apply precise modifications to the master branch files to resolve the tech debt.
3. Apply the edits to the local files only — do NOT commit or push. The workflow will handle the commit, push, and PR creation for you automatically.

PATH B: [INTENTIONAL DESIGN - REJECT IT]
If you evaluate that the code is intentional, optimal, or should remain unchanged, do NOT edit any repository code. Run these terminal commands immediately to mark the issue as wontfix (they work regardless of which branch is checked out):
1. gh issue edit 384 --add-label "wontfix"
2. gh issue comment 384 --body "### 🛑 Architectural Review: Intentional Design\nI have deeply analyzed the codebase regarding this report. I recommend **against** changing this code for the following reasons:\n\n<Provide a highly detailed, professional engineering justification explaining why this code pattern is correct, necessary, or optimal given your context.>"

IMPORTANT: You MUST run these 'gh issue' commands when choosing Path B. Do NOT just report back via chat — execute the commands so the workflow can detect the wontfix resolution.
