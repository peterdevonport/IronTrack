You are an autonomous Senior Software Engineer evaluating an on-demand code refactor request.
Target Issue: #407 - [OpenCode Technical Debt] Cross-referencing gap — skills duplicate rules with no canonical source

### REFACTOR BLUEPRINT REPORTED:

### Source
This issue was auto-detected from merged Pull Request #399 (refactor/opencode-architecture).

### Location
Found across: `.opencode/skills/universal/git-workflow/SKILL.md`, `.opencode/skills/disciplines/pr-workflow/SKILL.md`, `.opencode/skills/disciplines/qa-testing/SKILL.md`, `.opencode/skills/irontrack/irontrack-testing/SKILL.md`.

### Description
The modular skill architecture was designed to eliminate the monolithic AGENTS.md, but the skills now contain overlapping rules without a clear "single source of truth" pattern. Examples:

- Branch naming is in both `git-workflow` and `pr-workflow`
- Test commands are in both `qa-testing` and `irontrack-testing`
- Commit message format is in both `git-workflow` and `pr-workflow`
- The "never use closes/fixes #N" rule appears in two skills

The skills system provides no cross-referencing mechanism. When a rule exists in multiple places, there is no way to know which is authoritative. This is a structural DRY violation that will compound as more skills are added.

### Suggested Refactor
Adopt a clear ownership model:
- Each rule should live in exactly one skill (the canonical source)
- Other skills that need the rule should cross-reference it: "See git-workflow skill for branch naming and commit rules"
- Add a comment convention in skill files: `<!-- canonical: git-workflow -->` to mark which skill owns each section

### YOUR MISSION:
Analyze the corresponding source files in the current repository. Determine if this reported code smell is a genuine defect/anti-pattern that needs correcting, OR if this structure is intentional (e.g., required for performance, compliance with third-party APIs, or an explicitly guarded architectural design).

### EXECUTION DIRECTIONS (CHOOSE EXACTLY ONE PATH):

PATH A: [VALID CODE SMELL - FIX IT]
If the issue is correct and should be refactored:
1. Checkout a new branch named 'opencode/fix-issue-407'.
2. Apply precise modifications to the master branch files to resolve the tech debt.
3. Apply the edits to the local files only — do NOT commit or push. The workflow will handle the commit, push, and PR creation for you automatically.

PATH B: [INTENTIONAL DESIGN - REJECT IT]
If you evaluate that the code is intentional, optimal, or should remain unchanged, do NOT edit any repository code. Run these terminal commands immediately to mark the issue as wontfix (they work regardless of which branch is checked out):
1. gh issue edit 407 --add-label "wontfix"
2. gh issue comment 407 --body "### 🛑 Architectural Review: Intentional Design\nI have deeply analyzed the codebase regarding this report. I recommend **against** changing this code for the following reasons:\n\n<Provide a highly detailed, professional engineering justification explaining why this code pattern is correct, necessary, or optimal given your context.>"

IMPORTANT: You MUST run these 'gh issue' commands when choosing Path B. Do NOT just report back via chat — execute the commands so the workflow can detect the wontfix resolution.
