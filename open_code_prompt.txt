You are an autonomous Senior Software Engineer evaluating an on-demand code refactor request.
Target Issue: #403 - [OpenCode Technical Debt] Architectural regression — graphify enforcement guardrails removed from AGENTS.md and plugin deleted

### REFACTOR BLUEPRINT REPORTED:

### Source
This issue was auto-detected from merged Pull Request #399 (refactor/opencode-architecture).

### Location
Found in modified section of code: `AGENTS.md` (reduced from 74 to 17 lines), `.opencode/plugins/graphify.js` (deleted entirely).

### Description
The original `AGENTS.md` contained a mandatory graph-first rule with detailed justification:

> **You MUST query the graph before using read/grep/glob/bash for codebase exploration or relationship questions.** A graph query costs ~2k tokens. Reading or grepping raw files for the same information costs ~10k+ tokens.

This rule was enforced at runtime by the `graphify.js` plugin, which intercepted tool executions and reminded agents to use the knowledge graph. The plugin was deleted and the AGENTS.md rule was reduced to:

> Use Graphify before exploring existing architecture or relationships when available

This is a significant weakening of a guardrail. The original rule was **mandatory** (`MUST`); the new version is **advisory** (`when available`). The token-cost justification that motivated the rule was removed entirely. Without the plugin, there is no runtime enforcement — agents are free to skip the graph and burn 5x more tokens.

### Suggested Refactor
Restore the graph-first rule in `AGENTS.md` with its full justification (token cost differential). Consider either restoring the plugin for runtime enforcement or adding the rule to the `graphify-query` skill description so the skill-matching system surfaces it proactively.

### YOUR MISSION:
Analyze the corresponding source files in the current repository. Determine if this reported code smell is a genuine defect/anti-pattern that needs correcting, OR if this structure is intentional (e.g., required for performance, compliance with third-party APIs, or an explicitly guarded architectural design).

### EXECUTION DIRECTIONS (CHOOSE EXACTLY ONE PATH):

PATH A: [VALID CODE SMELL - FIX IT]
If the issue is correct and should be refactored:
1. Checkout a new branch named 'opencode/fix-issue-403'.
2. Apply precise modifications to the master branch files to resolve the tech debt.
3. Commit the changes, push the branch, and create a PR with title matching the issue title. Do NOT enable auto-merge — the workflow handles that.

PATH B: [INTENTIONAL DESIGN - REJECT IT]
If you evaluate that the code is intentional, optimal, or should remain unchanged, do NOT edit any repository code. Run these terminal commands immediately to mark the issue as wontfix (they work regardless of which branch is checked out):
1. gh issue edit 403 --add-label "wontfix"
2. gh issue comment 403 --body "### 🛑 Architectural Review: Intentional Design\nI have deeply analyzed the codebase regarding this report. I recommend **against** changing this code for the following reasons:\n\n<Provide a highly detailed, professional engineering justification explaining why this code pattern is correct, necessary, or optimal given your context.>"

IMPORTANT: You MUST run these 'gh issue' commands when choosing Path B. Do NOT just report back via chat — execute the commands so the workflow can detect the wontfix resolution.
