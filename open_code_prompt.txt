You are an autonomous Senior Software Engineer evaluating an on-demand code refactor request.
Target Issue: #389 - [OpenCode Technical Debt] Blocking synchronous existsSync in async plugin hook

### REFACTOR BLUEPRINT REPORTED:

### Source
This issue was auto-detected from merged Pull Request #388.

### Location
Found in modified section of code: `.opencode/plugins/graphify.js` (line 31).

### Description
The `tool.execute.before` hook is declared as `async` but uses `existsSync` (synchronous filesystem check) to verify whether `graphify-out/graph.json` exists. This blocks the Node.js event loop on every tool invocation (bash, read, grep, glob) that passes the first two guards. In an async plugin system, synchronous I/O defeats the purpose of async handlers and can cause performance degradation when the tool is invoked frequently.

### Suggested Refactor
Replace `existsSync` with the async equivalent using `fs/promises`:
```js
import { access } from "fs/promises";
// ...
if (!access(join(directory, "graphify-out", "graph.json")).catch(() => false)) return;
```
Or use a cached boolean that is set once on first invocation and refreshed periodically.

### YOUR MISSION:
Analyze the corresponding source files in the current repository. Determine if this reported code smell is a genuine defect/anti-pattern that needs correcting, OR if this structure is intentional (e.g., required for performance, compliance with third-party APIs, or an explicitly guarded architectural design).

### EXECUTION DIRECTIONS (CHOOSE EXACTLY ONE PATH):

PATH A: [VALID CODE SMELL - FIX IT]
If the issue is correct and should be refactored:
1. Checkout a new branch named 'opencode/fix-issue-389'.
2. Apply precise modifications to the master branch files to resolve the tech debt.
3. Apply the edits to the local files only — do NOT commit or push. The workflow will handle the commit, push, and PR creation for you automatically.

PATH B: [INTENTIONAL DESIGN - REJECT IT]
If you evaluate that the code is intentional, optimal, or should remain unchanged, do NOT edit any repository code. Run these terminal commands immediately to mark the issue as wontfix (they work regardless of which branch is checked out):
1. gh issue edit 389 --add-label "wontfix"
2. gh issue comment 389 --body "### 🛑 Architectural Review: Intentional Design\nI have deeply analyzed the codebase regarding this report. I recommend **against** changing this code for the following reasons:\n\n<Provide a highly detailed, professional engineering justification explaining why this code pattern is correct, necessary, or optimal given your context.>"

IMPORTANT: You MUST run these 'gh issue' commands when choosing Path B. Do NOT just report back via chat — execute the commands so the workflow can detect the wontfix resolution.
