You are an autonomous Senior Software Engineer evaluating an on-demand code refactor request.
Target Issue: #523 - [OpenCode Technical Debt] Bloated step_lint_test_fix_push() mixes 7 concerns in resolve-conflicts.sh

### REFACTOR BLUEPRINT REPORTED:

### Location
Found in recent master commits. File: `.github/scripts/resolve-conflicts.sh`, lines 70-113 (43 lines).

### Description
The `step_lint_test_fix_push()` function handles: npm dependency installation, ESLint execution, lint auto-fix with AI, test execution, test auto-fix with AI, conflict marker verification, git staging, git commit, and git push. This is a single function doing the work of at least 4 distinct operations. It violates the Single Responsibility Principle and makes the function difficult to test, modify, or reason about in isolation.

Each sub-operation (lint, test, commit) has its own error handling and retry-with-AI pattern, creating deeply nested control flow. If any step needs to change (e.g., adding a build step, changing test runner), the entire monolithic function must be understood.

### Suggested Refactor
Split into focused functions:
```bash
step_install_deps() {
  log "--- Step: Install Dependencies ---"
  npm ci
}

step_lint_with_autofix() {
  log "--- Step: Lint & Auto-Fix ---"
  if ! npm run lint; then
    warn "Lint failed. Attempting auto-fix with OpenCode..."
    timeout 300 opencode run --auto --model opencode-go/deepseek-v4-flash       "Fix the SOURCE CODE to resolve all ESLint errors. Do NOT modify test files." ||       { fail "OpenCode timed out after 5 minutes"; return 1; }
    npm run lint || { fail "Lint still failing after fix."; return 1; }
  fi
}

step_test_with_autofix() { /* similar */ }
step_verify_and_push() { /* similar */ }
```
Then compose them in main: `step_install_deps && step_lint_with_autofix && step_test_with_autofix && step_verify_and_push`.

### YOUR MISSION:
Analyze the corresponding source files in the current repository. Determine if this reported code smell is a genuine defect/anti-pattern that needs correcting, OR if this structure is intentional (e.g., required for performance, compliance with third-party APIs, or an explicitly guarded architectural design).

### EXECUTION DIRECTIONS (CHOOSE EXACTLY ONE PATH):

PATH A: [VALID CODE SMELL - FIX IT]
If the issue is correct and should be refactored:
1. Checkout a new branch named 'opencode/fix-issue-523'.
2. Apply precise modifications to the master branch files to resolve the tech debt.
3. Commit the changes, push the branch, and create a PR with title matching the issue title. Do NOT enable auto-merge — the workflow handles that.

PATH B: [INTENTIONAL DESIGN - REJECT IT]
If you evaluate that the code is intentional, optimal, or should remain unchanged, do NOT edit any repository code. Run ONLY these two terminal commands to mark the issue as wontfix (they work regardless of which branch is checked out):
1. gh issue edit 523 --add-label "wontfix"
2. gh issue comment 523 --body "### 🛑 Architectural Review: Intentional Design\nI have deeply analyzed the codebase regarding this report. I recommend **against** changing this code for the following reasons:\n\n<Provide a highly detailed, professional engineering justification explaining why this code pattern is correct, necessary, or optimal given your context.>"

CRITICAL: Do NOT create any branch, commit, push, or pull request. Do NOT edit any files. Run ONLY the two gh issue commands above. If you create a PR, the workflow will auto-merge it because it cannot distinguish wontfix PRs from real fixes.
